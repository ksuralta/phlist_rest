language: python
python:
  - "2.7"
env:
  - DJANGO=1.6.5 DB_ENGINE="django.db.backends.sqlite3" DB_NAME="phlistdb"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: python manage.py test